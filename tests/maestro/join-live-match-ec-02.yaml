appId: ${APP_ID}
---
# GENERATED by Blume compiler — do not edit by hand.
# Spec: join-live-match@v1 / EC-02
# Seed: abcdef0123456789

- launchApp
- runFlow:
    file: setup.yaml # the user taps "Join"; User is authenticated; A live match exists and is joinable
- extendedWaitUntil:
    timeout: 2000
- assertVisible: "Retry"
# assert noPartialState: no partial join state is left behind
